# EcoVision Climate Visualizer - Environment Configuration
# =========================================================
# 
# This file shows the available configuration options.
# The application works OUT OF THE BOX with the defaults shown below.
# 
# You only need to create a .env file if your setup differs from these defaults.
#
# To customize:
#   1. Copy this file: cp .env.example .env
#   2. Edit .env with your actual values
#   3. The .env file is git-ignored (safe for passwords)
#

# Database Configuration
# ----------------------
# Default values work for standard local MySQL installation
MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DB=climate_data

# Flask Configuration
# -------------------
FLASK_ENV=development
FLASK_DEBUG=True

# Notes:
# - Empty MYSQL_PASSWORD works for MySQL installed without a root password
# - If you set a root password during MySQL installation, add it to MYSQL_PASSWORD
# - For Docker deployments, override these in docker-compose.yml or runtime environment
